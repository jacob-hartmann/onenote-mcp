## OneNote MCP environment variables
##
## Copy this file to `.env` and fill in the values.
## NOTE: `.env` is gitignored (do not commit secrets).

# ─────────────────────────────────────────────────────────────────────────────
# Microsoft OAuth credentials (required unless ONENOTE_ACCESS_TOKEN is set)
# ─────────────────────────────────────────────────────────────────────────────

# Azure Entra app (client/application) ID
ONENOTE_OAUTH_CLIENT_ID=

# Azure Entra app client secret
ONENOTE_OAUTH_CLIENT_SECRET=

# ─────────────────────────────────────────────────────────────────────────────
# Optional manual token override (skips OAuth flow)
# ─────────────────────────────────────────────────────────────────────────────

# If set, OAuth is bypassed and this token is used directly.
# ONENOTE_ACCESS_TOKEN=

# ─────────────────────────────────────────────────────────────────────────────
# OAuth behavior
# ─────────────────────────────────────────────────────────────────────────────

# OAuth tenant. Defaults to multi-tenant "common".
# Examples: common, organizations, consumers, <tenant-id>
# ONENOTE_OAUTH_TENANT=common

# Redirect URI for interactive OAuth callback server.
# Must match the redirect URI configured in your Azure app.
ONENOTE_OAUTH_REDIRECT_URI=http://localhost:3000/callback

# Space-delimited scopes requested during OAuth.
# Defaults to: offline_access openid profile User.Read Notes.ReadWrite
# Notes.ReadWrite is required for tools that create, update, or delete pages and sections.
# ONENOTE_OAUTH_SCOPES=offline_access openid profile User.Read Notes.ReadWrite

# Token cache file path (optional, uses platform default if not set)
# Default locations:
#   Windows: %APPDATA%\onenote-mcp\tokens.json
#   macOS:   ~/Library/Application Support/onenote-mcp/tokens.json
#   Linux:   ~/.config/onenote-mcp/tokens.json
# ONENOTE_TOKEN_STORE_PATH=

# Optional base URL override for Microsoft identity authority
# ONENOTE_OAUTH_AUTHORITY_BASE_URL=https://login.microsoftonline.com

# Optional base URL override for Microsoft Graph
# ONENOTE_GRAPH_BASE_URL=https://graph.microsoft.com/v1.0

# ─────────────────────────────────────────────────────────────────────────────
# Transport mode (default: stdio)
# ─────────────────────────────────────────────────────────────────────────────

# Transport mode. Set to "http" for HTTP+SSE with OAuth (e.g., for MCP Inspector).
# Default is "stdio" for direct LLM integration.
# MCP_TRANSPORT=http

# HTTP server host (default: 127.0.0.1)
# MCP_SERVER_HOST=127.0.0.1

# HTTP server port (default: 3001)
# MCP_SERVER_PORT=3001

# OAuth issuer URL (default: http://localhost:{port})
# MCP_ISSUER_URL=http://localhost:3001
